<h1>Catalog </h1>

<div class= "cards">
  <% @games.each do |game|  %>
  <div class="g-card"> 
    <%= image_tag  "gfn-placeholder.jpg", class: "cover-img" %>
    <p><%= game["title"] %></p> 
  </div>
  <% end %>
</div>